<h1>
    <%= image_tag current_user.avatar.url %>
    <%= current_user.name %>
</h1>
<span><%= link_to "view my profile", current_user %></span>
<span><%= pluralize(current_user.foodposts.count, "foodpost") %></span>