<h1>投稿の編集</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@foodpost) do |f| %>
      <%= render 'shared/error_messages', object: f.object %>

        <%= image_tag @foodpost.picture.url %>
        <%= f.file_field :picture, accept: 'image/jpeg,image/gif,image/png' %>

        <%= f.label :protein, "タンパク質" %><br>
        <%= f.number_field :protein, min: 0, max: 999, class: "pfc_field" %> g(1g = 4kcal)<br>
        
        <%= f.label :fat, "脂質" %><br>
        <%= f.number_field :fat, min: 0, max: 999, class: "pfc_field" %> g(1g = 9kcal)<br>
        
        <%= f.label :carbohydrate, "炭水化物" %><br>
        <%= f.number_field :carbohydrate, min: 0, max: 999, class: "pfc_field" %> g(1g = 4kcal)<br>
        
        <%= f.text_area :content %>
        
      <%= f.submit "更新する", class: "btn btn-success" %>
    <% end %>
    <%= link_to "トップページに戻る", root_path %>
  </div>
</div>

<script type="text/javascript">
  $('#foodpost_picture').bind('change', function() {
    var size_in_megabytes = this.files[0].size/1024/1024;
    if (size_in_megabytes > 5) {
      alert('Maximum file size is 5MB. Please choose a smaller file.');
    }
  });
</script>