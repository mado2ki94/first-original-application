<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= @user.name %>
      </h1>
    </section>
  </aside>
  <div class="col-md-8">
    <% if @user.foodposts.any? %>
      <h3><%= @user.name %>さんの投稿一覧(<%= @user.foodposts.count %>)</h3>
      <ol class="foodposts">
        <%= render @foodposts %>
      </ol>
      <%= will_paginate @foodposts %>
    <% end %>
  </div>
</div>